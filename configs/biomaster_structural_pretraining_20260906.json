{
  "status": "FROZEN_BEFORE_STRUCTURAL_TRAINING",
  "cutoff": 2020,
  "seed": 20260921,
  "model_config": "configs/biomaster_pocket_precision_20260906.json",
  "max_epochs": 8,
  "patience": 2,
  "microbatch_size": 2,
  "effective_batch_size": 16,
  "learning_rate": 0.0001,
  "weight_decay": 0.01,
  "warmup_updates": 200,
  "gradient_clip": 1.0,
  "loss_weights": [1.0, 1.0, 0.2],
  "losses": ["64-bin observed distance cross entropy balanced at 16A", "contact BCE balanced at 4.5A", "native-region assignment cross entropy"],
  "selection": "internal cluster-held-out macro contact AP",
  "validation_every_updates": 200,
  "precision": "BF16 trunk, FP32 losses; TF32 disabled",
  "dataset": "structural_data/training_2020/MANIFEST.json",
  "input_ligand_geometry": "independent ETKDG with MMFF/UFF optimization",
  "contact_labels": "experimental bound pose only, all mapped protein chains, complete heavy-atom pocket residues",
  "test_scope": "internal structural validation only; not drug-target ranking AP",
  "early_cutoff_reuse": "2020 pretraining cannot initialize a 2018 downstream model"
}
