# DTIAM 进度记录（2026-09-13）

核对时间：2026-09-13T09:51:28.293279+00:00。完整拟合及公共验证仍为 **1/6 组**，统一 TEST 尚未启动。

| 组别 | 实际进度 |
|---|---|
| A / 20260921 | 11 个基础学习器和加权集成完成；公共验证完成 |
| B / 20260921 | LightGBMXT 已完成，第二个 LightGBM 正在独立进程训练；其余待运行 |
| A / 20260922、20260923 | 待运行 |
| B / 20260922、20260923 | 待运行 |

B 的 LightGBMXT 已完成 10,000 轮原定预算，原生拟合耗时 40.23 分钟；这不代表证明收敛。其 0.9804 是原生内部留出 ROC-AUC，不是公共验证或 TEST 成绩。本轮没有新增公共验证成绩，不能据此判断 B 是否优于 A。

队列于 09:03 UTC 在第二个 LightGBM 的内存预检查处退出：估计另需 46.782 GiB，进程当时只剩 47.585 GiB 的可用估计，不满足安全余量。实际没有报告容器 OOM；首个模型权重已成功保存。仅提高整个核心阶段软预算未解决连续模型间的内存驻留问题。

09:48 UTC 已恢复为 B 的每个核心学习器各用一个新进程，通过原生 `fit_extra` 复用同一训练缓存和内部划分。第二个模型已实际进入拟合，其预检查可用估计为 74.6 GiB。核心软预算维持 84 GiB，实际子进程 RSS 保护线维持 78 GiB。十个核心学习器仍全部必需，随后继续原独立 FastAI 和最终加权集成。

已核对 1,116,270 个训练成员的缓存标签与身份；基础训练 1,105,107 行，内部留出 11,163 行。首个 B 子模型的权重哈希与恢复前一致，已完成的 A 继续复用。新增两个测试验证原生保存/加载/继续拟合后旧模型字节、预测、内部成员不变，以及完整默认超参数覆盖。大规模权重与缓存、失败现场留在本地，资源与代码变更的前后哈希有审计记录。

独立进程初始化为本组种子，各原生子模型保留自己的种子规则；不声称与不中断单进程执行逐位一致。数据划分、标签、模型学习参数、公共验证选择规则与 TEST 门控未改。

先前 30—60 小时只是粗估，连续中断使固定完成时间不再可靠；待首个完整 B 组结束再校正剩余耗时。当前优先完成整组训练和公共验证，而不是用单个子模型的内部高分替代整组结果。

阶段性能见 [A 阶段评价](BIOMASTER_DTIAM_INTERIM_REVIEW_20260913_ZH.md)，实时状态见 [STATUS.json](../outputs/biomaster_dtiam_ab_20260912/STATUS.json)，本次快照见 [PROGRESS_20260913.json](../outputs/biomaster_dtiam_ab_20260912/PROGRESS_20260913.json)。
