# DTIAM 进度与阶段评价（2026-09-14）

核对时间：2026-09-14 00:03 UTC（北京时间 08:03）。**队列连续运行约 8.3 小时，B 首组已完成 10/11 个基础学习器，正在训练最后的 FastAI。完整拟合及公共验证仍完成 1/6 组，尚无新增 B 公共验证或统一 TEST 成绩。**

| 组别 | 已核实进度 |
|---|---|
| A / 20260921 | 11 个基础学习器、集成和公共验证已完成 |
| B / 20260921 | 十个核心学习器完成，FastAI 训练中；随后集成、公共验证 |
| A / 20260922、20260923 | 待运行 |
| B / 20260922、20260923 | 待运行 |

与上次相比，新完成三个较耗时阶段：

| 新完成的 B 学习器 | 成功阶段耗时 | 停止与保留权重 |
|---|---:|---|
| XGBoost | 2.92 小时 | 日志到 9,999，即完成 10,000 轮预算；保存 7,959 棵树的最佳模型 |
| NeuralNetTorch | 4.13 小时 | 日志实际到第 291 轮，保留第 181 轮最佳权重 |
| LightGBMLarge | 1.01 小时 | 日志完成 10,000 轮预算；保留 9,972 轮模型 |

三个新模型的已保存文件哈希与阶段记录一致。B 十个核心学习器的成功阶段累计耗时 **10.81 小时**，包含初始化、保存及核验等开销，排除失败尝试与停机；因此不等于从本次恢复以来的墙钟时间。

FastAI 于 9 月 13 日 23:49 UTC 启动，目前日志已报告第 11 轮的验证损失改善，原生无时间截断设置对应最多 30 轮。日志只打印改善事件，不能将最后改善轮数直接当成精确当前轮数。队列 PID 2015、当前子进程 PID 16230 均存活，FastAI RSS 约 34.6 GiB，日志持续推进；当前容器的 OOM 计数为 0。状态文件中的旧时间是进入本组时写入的时间，应结合 `CURRENT_CHILD.json`、子进程和最新日志判断是否仍在运行。

**阶段评价：运行稳定性已有实际进展，性能判断仍等待 B 公共验证。** 昨天尚未完成的 XGBoost、NeuralNetTorch 已在全量 B 上成功结束，现有资源安排已支持这些阶段。Torch 在最佳轮之后又训练了约 110 轮，可看到内部验证平台期；LightGBMLarge 的最佳迭代接近预算上限，仍不能把整套模型统一描述为“已证明全部收敛”。这里保持冻结预算和评估协议。

已有的 A 阶段证据仍然成立，但没有新增性能结果：在同一个公共 Kd/Ki 验证集上，DTIAM A 排序优选模型的 AP 为 0.9780，现有 BioMaster A 二分类为 0.9665；双向查询 AP 分别为 0.7184、0.7115。明确失活误报率分别为 12.49%、35.40%，对应 Kd/Ki 召回率为 94.97%、91.51%，各用自己的冻结验证阈值。排序优势较小且分方向不一致，例如 DTIAM A 排序优选的药物内 P@5 为 0.6474，低于现有联合排序模型的 0.6737。上述均为单套验证比较，不能用来声称三种子统一测试已胜出，或推算 SPR384 的命中率。详细比较见 [A 阶段评价](BIOMASTER_DTIAM_INTERIM_REVIEW_20260913_ZH.md)。

根据新增实测更新 ETA，从本次核查起：

| 里程碑 | 预计剩余时间 |
|---|---:|
| 首组 B 的 FastAI、集成与公共验证 | 约 0.5—1.5 小时 |
| 全六组、统一 TEST 与报告 | 约 24—40 小时，中心约 31 小时 |

全轮中心预计为 **9 月 15 日 07:00 UTC（北京时间 15:00）**，与昨日中心估计基本相符。依据为 A 全套实测 3.00 小时，B 核心阶段实测 10.81 小时；B 完整 FastAI＋集成暂留 0.75 小时、每套 B 公共验证暂留 0.25 小时，全轮测试与报告另留 0.5 小时。上述未完成阶段仍是估计，后续种子的早停轮数也会变化；范围是无再次中断前提下的工作安排，不是保证时限。首组 B 完成后再用完整耗时修正。

系统盘可用约 **10.68 GiB**，占用 65%；数据盘可用约 **68.5 GiB**。昨日释放的系统盘空间保持稳定。每套公共验证完成后，队列会按原设计清理本套可重建特征表缓存，保留模型、内部划分与预测文件。

当前最有价值的下一项结果是：B 在相同公共 Kd/Ki 验证成员上的双向排序表现，以及明确失活误报、IC50/EC50 表现。六套拟合及验证选择全部冻结后才进入统一 TEST，再决定是否升级模型。

可核验快照及 ETA 公式见 [PROGRESS_20260914.json](../outputs/biomaster_dtiam_ab_20260912/PROGRESS_20260914.json)，实时任务见 [STATUS.json](../outputs/biomaster_dtiam_ab_20260912/STATUS.json) 与 [CURRENT_CHILD.json](../outputs/biomaster_dtiam_ab_20260912/CURRENT_CHILD.json)。本轮没有改训练参数、数据、生产模型、网站或 SPR 交付清单。
