# 磁盘清理执行完成（2026-09-10）

经用户确认“好的，清理这些”，已按上一轮推荐的约 101.65 GB 逐文件清单执行完成。没有扩大到其他候选目录，也没有删除 DTIAM 历史折模型。

| 指标 | 实际结果 |
|---|---:|
| 文件清单所对应的可回收块空间 | 101.647 GB / 94.666 GiB |
| 实测磁盘可用空间增量 | **101.612 GB / 约 94.63 GiB** |
| 执行前可用空间 | 17.875 GB |
| 执行后可用空间 | **119.487 GB / 约 111.28 GiB** |
| 磁盘占用比例 | 97% → 80% |
| 删除的清单内文件 | 272,487 |
| 以硬链接去重、保持原路径可用的文件 | 7 |
| 移除的空目录 | 23,739 |

实测增量与文件块空间估计略有差别，受目录元数据和同盘并发活动影响。没有为了补足差值追加删除。

清理内容包括旧 Boltz PAE/PDE 和部分 processed、已归档的旧离线包展开副本、完整 BindingDB ZIP 对应的下载分片、旧原子特征、停止的结构/context 分支派生特征，以及指定旧试跑和工程检查点。原始结构、标签、划分、汇总结果和正式保留模型仍在。恢复已停止分支时，需要重新生成被清理的特征；历史审计中“文件已生成”的记录描述当时状态，不表示这些大文件目前仍存于本地。

执行前重新校验了全部 272,494 条文件的大小、mtime、设备、inode 和磁盘块占用。重新核对重复文件 SHA256、离线 ZIP 完整性及展开内容 CRC，并确认 BindingDB 分片拼接与保留 ZIP 的 SHA256 相同。删除只使用被冻结清单内的文件路径，没有对父目录做递归删除；目录仅在确认为空后移除。

7 个重复副本通过同一文件系统的硬链接共享保留实体，原路径继续存在，内容校验通过。以后若要改写其中某份模型或特征，应采用生成新文件后替换路径的方式，避免原地写入共享 inode。

清理后的验证均通过：

- 372 个受保护文件的 SHA256 前后一致，包括最终实验表、对照、生产包和冻结评分来源。
- 全部 DTIAM 折目录中 682 个文件的大小、mtime、设备与 inode 前后一致。
- 当前湿实验候选及新旧对照关联的 Boltz 保护规则始终生效。
- 生产模型包完整性校验通过，CPU 重新加载后，药物→靶点和靶点→药物两个方向实际推理成功。
- 网站数据在新进程中从磁盘重新加载成功：720 药、888 靶点、276,480 已评分配对，无加载警告；最终实验表仍为 384 候选和 112 对照。
- 湿实验回填 SQLite 的 `PRAGMA quick_check` 返回 `ok`。
- 现有网站首页返回 HTTP 200，未认证 API 仍返回 401；没有重启或修改网站认证。
- SPR 最终表、对照、回填及综合设计相关回归测试：**23 passed**。

执行记录：

- [执行结果](../outputs/disk_reclamation_execution_20260910/RESULT.json)
- [服务及模型验证](../outputs/disk_reclamation_execution_20260910/SERVICE_VERIFICATION.json)
- [执行清单快照](../outputs/disk_reclamation_execution_20260910/EXECUTED_PLAN.csv.gz)
- [逐文件操作流水](../outputs/disk_reclamation_execution_20260910/FILE_ACTIONS.csv)
- [执行前核验](../outputs/disk_reclamation_execution_20260910/PREFLIGHT.json)

原审计清单现为历史快照，不能再次直接执行。执行脚本固定了审批清单 SHA256，并拒绝在已有执行开始记录的情况下盲目重跑。
